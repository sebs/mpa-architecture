{
  "name": "mpa-architecture",
  "version": "0.1.0",
  "private": true,
  "description": "Pure MPA reference implementation: a website with a database, no frameworks, no build step",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "serve": "python3 -m http.server 8080"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebs/mpa-architecture.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "banira": "^0.6.0",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^29.1.1"
  }
}
